@model Ginosis.Banat.MvcWebUI.Models.Authority.AuthorityListViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage2.cshtml";
}

<div class="card" id="newRoleForm">
    <div class="card-header bg-white header-elements-inline">
        <h5 class="card-title"><strong>İşlem Yetkileri</strong></h5>
    </div>
    
    <div class="table-responsive">
        <table class="table table-bordered table-hover table-responsive table-striped">
            <tr>
                <th>Id</th>
                <th>Role</th>
                <th></th>
            </tr>
            @foreach (var item in Model.Authorities)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.AuthorityName</td>
                    <td><a href="/Authority/Delete?authorityId=@item.Id" class="btn-sm btn-danger"><i class="fa fa-edit"></i> Sil </a></td>
                </tr>
            }
        </table>

        <a href="/Authority/Add" class="btn btn-success">Yeni Rol Ekle</a>

    </div>

</div>


