@model Ginosis.Banat.MvcWebUI.Models.AuthorityRole.AuthorityRoleListViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage2.cshtml";
}

<div class="card" id="newRoleForm">
    <div class="card-header bg-white header-elements-inline">
        <h5 class="card-title"><strong>İşlem Yetkileri</strong></h5>
    </div>
    
    <div class="table-responsive">
        <table class="table table-bordered table-hover table-responsive table-striped">
            <tr>
                <th>Id</th>
                <th>Role</th>
                <th></th>
            </tr>
            @foreach (var item in Model.Roles)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td><a href="/AuthorityRole/Update?roleId=@item.Id" class="btn-sm btn-danger"><i class="fa fa-edit"></i> Guncelle </a></td>
                </tr>
            }
        </table>


    </div>

</div>


