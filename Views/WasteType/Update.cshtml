@model Ginosis.Banat.MvcWebUI.Models.WasteType.WasteTypeUpdateViewModel
@{
    ViewBag.Title = "Add";
    Layout = "~/Views/_LayoutPage2.cshtml";
}

<div class="card" id="updateWasteTypeForm">
    <div class="card-header bg-white header-elements-inline">
        <h5 class="card-title"><strong>Fire tipi Güncelleme Sayfası</strong></h5>
    </div>
    <div class="card-body">
        @using (Html.BeginForm("Update", "WasteType", FormMethod.Post))
        {
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.AntiForgeryToken()        

            <div class="form-group row">
                <div class="col-lg-4">
                    <div class="row">
                        <label class="col-md-3 col-form-label">Açıklama</label>
                        <div class="col-md-9">
                            @Html.TextBoxFor(x => x.WasteType.Description, new { @class = "form-control", @placeholder = "Açıklama" })
                        </div>
                    </div>
                </div>
            </div>

            <div class="text-right">
                <button type="submit" class="btn btn-primary">Güncelle</button>
            </div>

        }
    </div>
</div>


